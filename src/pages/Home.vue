<template>
  <main class="home">
    <section class="placeholder">
      <h1>Gino Yu</h1>
      <p>網站重建中 — Day 1：骨架與設計變數</p>
    </section>
  </main>
</template>

<style scoped>
.home {
  max-width: 960px;
  margin: 0 auto;
  padding: var(--space-8) var(--space-2);
}

.placeholder {
  border-left: 4px solid var(--color-accent);
  padding-left: var(--space-2);
}

.placeholder p {
  margin-top: var(--space-1);
  color: var(--color-muted);
}
</style>
